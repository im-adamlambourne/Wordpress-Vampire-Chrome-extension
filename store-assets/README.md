# Store listing assets

Files used in the Chrome Web Store dashboard, not in the extension ZIP.

| File | Use |
|------|-----|
| `store-icon-128.png` | Store listing icon (same as `icons/icon-128.png`) |
| `screenshot-1.png` … | 1280×800 captures — see `CHROMEWEBSTORE.md` |

Take screenshots from a real signed-in editor session. Do not ship this folder in `scripts/package-cws.sh`.
